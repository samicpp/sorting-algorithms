import * as check from "./check.ts";

export const radixSort=new class RadixSort{
    operations=0;
    
    number(arr: number[]):number[]{
        const sorted:number[]=[];
        
        return sorted;
    }
}